// com.example.travelvault.ui.theme/Color.kt
package com.example.travelvault.ui.theme

import androidx.compose.ui.graphics.Color

// Default template colors (we can leave them)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// --- ADDING OUR NEW DARK THEME COLORS ---

// A very dark, slightly purple-blue, just like your 'Moctale' reference
val AppDarkBackground = Color(0xFF1C1B22)

// A slightly lighter gray-purple for cards and surfaces
val AppDarkSurface = Color(0xFF2A2931)

// A vibrant, premium purple for buttons and highlights
val AppPrimaryAccent = Color(0xFFBB86FC)

// A clean, bright white for text
val AppText = Color(0xFFFFFFFF)

// A slightly muted gray for secondary text
val AppSecondaryText = Color(0xFFB0B0B0)